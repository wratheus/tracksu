name: tracksu_storage
description: Secure local storage contracts for Tracksu.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^11.0.0
  meta: ^1.19.0

dev_dependencies:
  flutter_lints: ^6.0.0
