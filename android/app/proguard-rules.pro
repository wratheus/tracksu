# App-specific R8/ProGuard rules.
#
# Flutter and its plugins contribute their own keep rules. Add a narrow rule
# here only after a verified release/profile shrinker failure; do not disable
# shrinking globally as a workaround.
